"use strict";
Object.defineProperty(exports, "__esModule", {
    value: true
});
exports.normalizeViewPath = normalizeViewPath;
function normalizeViewPath(pathname) {
    let normalized = '';
    const segments = pathname.split('/');
    segments.forEach((segment, index)=>{
        if (!segment) return;
        if (segment.startsWith('(') && segment.endsWith(')')) {
            return;
        }
        if (segment === 'page' && index === segments.length - 1) {
            return;
        }
        normalized += `/${segment}`;
    });
    return normalized;
}

//# sourceMappingURL=view-paths.js.map